header:
  - titles:
      # @start locale config
      en      : &EN      Submit Data
      # @end locale config
    url: /sda-submission-guide.html

  - titles:
      # @start locale config
      en      : &EN       Access Data
      # @end locale config
    url: /data-access.html
  
  - titles:
      # @start locale config
      en      : &EN       Privacy Notice
      # @end locale config
    url: /privacy-notice.html

  
  - titles:
      # @start locale config
      en      : &EN       Contact
      # @end locale config
    url: /contact.html
